apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: webserver
spec:
  replicas: 1
  template:
    metadata:
      labels:
        name: webserver
    spec:
      containers:
      - image: angmerica/todo-client:webserver-develop-${CIRCLE_SHA1}
        name: webserver
        ports:
        - name: http-server
          containerPort: 80
          hostPort: 80
        env:
        - name: NODE_ENV
          value: "integration"
